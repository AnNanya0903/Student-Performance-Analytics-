# Copy this file to .env if you want to override any defaults.
# The app works out-of-the-box with SQLite and needs no secrets to run.

# Path to the SQLite database file (relative to project root)
DATABASE_PATH=database/student_analytics.db

# Default dataset used when "Sample Dataset" is selected in the sidebar
DEFAULT_DATASET_PATH=data/students.csv
